import { Typography, Input, Modal, DropNotifierActions } from '.'

describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(Input).toBeTruthy()
  })

  it('is truthy', () => {
    expect(Typography).toBeTruthy()
  })

  it('is truthy', () => {
    expect(Modal).toBeTruthy()
  })

  it('is truthy', () => {
    expect(DropNotifierActions).toBeTruthy()
  })
})
